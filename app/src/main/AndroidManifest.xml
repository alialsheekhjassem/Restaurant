<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="magma.global.restaurant">

    <!-- Include required permissions for Google Mobile Ads to run. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MAGMA"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Restaurant">

        <activity
            android:name=".presentation.select_language.SelectLanguageActivity" />
        <activity
            android:name=".presentation.select_city.SelectCityActivity" />
        <activity
            android:name=".presentation.select_region.SelectRegionActivity" />
        <activity
            android:name=".presentation.enter_code.EnterCodeActivity" />
        <activity
            android:name=".presentation.login.LoginActivity" />
        <activity
            android:name=".presentation.onboarding.OnboardActivity" />
        <activity
            android:name=".presentation.welcome.WelcomeActivity" />

        <activity
            android:name=".presentation.splash.SplashActivity"
            android:noHistory="true"
            android:exported="true"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>